# AGENTS.md

このリポジトリでは、`.claude/skills/**/SKILL.md` をエージェントスキルの正本とする。
git read-only probe は raw git より probe script を優先する。
repo-approved local temporary workspace は `tmp/` と `.claude/tmp/` を正本とし、変更時は `docs/dev/repository-folder-policy.md` を更新する。
