{
  "fixture_id": "secrets_mode",
  "description": "Public checkpoint branch present (via ls-remote exit=0 + GH_VISIBILITY=public) — triggers Kill Switch. NOTE: SRRS_SECRETS_MODE is not currently supported by check_session_recording_runtime_safety.py; this fixture tests the public-branch trigger path instead. SRRS_SECRETS_MODE support requires changes to Allowed Paths outside #380 scope — tracked as follow-up.",
  "srrs_overrides": {
    "SRRS_GIT_LS_REMOTE_EXIT": "0",
    "SRRS_GH_VISIBILITY": "public",
    "SRRS_GIT_CONFIG_OUTPUT": "",
    "SRRS_CHECKPOINT_TOKEN": "absent"
  },
  "expected_exit": "nonzero",
  "trigger": "public_checkpoint_branch_present",
  "expected_triggers": [
    "FAIL:public_checkpoint_branch_present"
  ]
}
