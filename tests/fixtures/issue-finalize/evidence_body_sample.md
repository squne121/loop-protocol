## no-diff finalize evidence

This Issue's Acceptance Criteria are already satisfied by the current
implementation on `main`; no code changes were required.

- AC1: verified via `uv run --locked pytest tests/test_example.py::test_ac1 -q`
