# LOOP_PROTOCOL Spec Kit Constitution Pointer

This file is a derived pointer retained only to keep Spec Kit's expected
constitution file path present.
It is not normative and must not be used as the policy source for
`/speckit-constitution`.

Canonical constitution, SDD tooling policy, and product specification rules live in:

- [ADR 0002: SDD tool adoption](../../docs/adr/0002-sdd-tool-adoption.md)
- [Product Spec Lifecycle](../../docs/dev/product-spec-lifecycle.md)
- [`docs/product/**`](../../docs/product/)

Conflict rule: if this file or any `.specify/**` artifact conflicts with `docs/**`,
`docs/**` wins.

Mutation rule: do not edit this file as a source of policy. Update canonical docs through
GitHub Issue / PR, then refresh this pointer if needed.

Operational note: `/speckit-constitution` treats this path as an update target
and may rewrite `.specify/memory/constitution.md`.
If invoked, treat the result as generated artifact drift and restore or update
this derived pointer through a reviewed PR.
