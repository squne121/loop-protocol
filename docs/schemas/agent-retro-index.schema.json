{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/squne121/loop-protocol/docs/schemas/agent-retro-index.schema.json",
  "title": "agent_retro_index/v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "generation_verdict",
    "entries",
    "orphan_reports",
    "ambiguous_links"
  ],
  "properties": {
    "schema": {
      "type": "string",
      "const": "agent_retro_index/v1"
    },
    "retrospective_runs": {
      "type": "array",
      "description": "Optional, additive-only derived section (Issue #2238 Child 5) summarizing agent-retrospective run publication comments discovered while building this index. Distinct from `entries[]` (agent_run_report/v1-derived) and never a state store -- the primary record for a run remains the agent_retrospective_run_publication/v1 Issue comment persist_retrospective_run.py posts (docs/adr/0007-agent-retrospective-boundaries.md Decision 7). Omitting this key entirely is valid (older index artifacts remain schema-valid).",
      "items": {
        "$ref": "#/$defs/retrospectiveRunEntry"
      }
    },
    "generation_verdict": {
      "type": "string",
      "enum": [
        "complete",
        "partial",
        "blocked"
      ]
    },
    "entries": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/entry"
      }
    },
    "orphan_reports": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/orphanReport"
      }
    },
    "ambiguous_links": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ambiguousLink"
      }
    }
  },
  "$defs": {
    "ambiguousLink": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "report_digest",
        "reason"
      ],
      "properties": {
        "report_digest": {
          "$ref": "#/$defs/sha256Digest"
        },
        "reason": {
          "type": "string",
          "minLength": 1,
          "maxLength": 160
        }
      }
    },
    "entry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "report_comment_url",
        "report_digest",
        "issue",
        "pr",
        "merge_sha",
        "tags",
        "friction_summary",
        "quality_signals",
        "follow_up_issues"
      ],
      "properties": {
        "report_comment_url": {
          "type": "string",
          "format": "uri",
          "maxLength": 240,
          "pattern": "^https://github\\.com/squne121/loop-protocol/(issues|pull)/[0-9]+#issuecomment-[0-9]+$"
        },
        "report_digest": {
          "$ref": "#/$defs/sha256Digest"
        },
        "issue": {
          "type": "integer",
          "minimum": 1
        },
        "pr": {
          "type": "integer",
          "minimum": 1
        },
        "merge_sha": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 60
          }
        },
        "friction_summary": {
          "type": "string",
          "minLength": 1,
          "maxLength": 240
        },
        "quality_signals": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          }
        },
        "follow_up_issues": {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 1
          }
        }
      }
    },
    "retrospectiveRunEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "run_comment_url",
        "run_digest",
        "base_sha",
        "source_set_digest",
        "candidate_count",
        "delta_summary"
      ],
      "properties": {
        "run_comment_url": {
          "type": "string",
          "format": "uri",
          "maxLength": 240,
          "pattern": "^https://github\\.com/squne121/loop-protocol/(issues|pull)/[0-9]+#issuecomment-[0-9]+$"
        },
        "run_digest": {
          "$ref": "#/$defs/sha256Digest"
        },
        "base_sha": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$"
        },
        "source_set_digest": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "candidate_count": {
          "type": "integer",
          "minimum": 0
        },
        "delta_summary": {
          "type": "string",
          "minLength": 1,
          "maxLength": 240
        }
      }
    },
    "orphanReport": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "report_digest",
        "reason"
      ],
      "properties": {
        "report_digest": {
          "$ref": "#/$defs/sha256Digest"
        },
        "reason": {
          "type": "string",
          "minLength": 1,
          "maxLength": 160
        }
      }
    },
    "sha256Digest": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    }
  }
}
