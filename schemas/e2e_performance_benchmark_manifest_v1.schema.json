{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://loop-protocol.local/schemas/e2e_performance_benchmark_manifest_v1.schema.json",
  "title": "E2E Performance Benchmark Manifest V1",
  "description": "Issue #2159 AC1/AC2/AC7: immutable manifest produced by scripts/ci/collect_e2e_performance_benchmark.py for a fixed before/after commit SHA e2e performance benchmark experiment. Each arm's evidence is a cross-run collector output (workflow-run-id-deduped, artifact-ID/digest/head-SHA/job verified) covering the jobs relevant to the pre-split `e2e` job and the post-split `e2e-core` / `e2e-responsive-matrix` jobs. This schema is intentionally immutable-by-convention: a manifest, once verified complete (arms[*].complete == true for all required jobs), must not be silently mutated in place -- a re-run produces a new manifest file (see docs/dev/e2e-performance-benchmark.md).",
  "type": "object",
  "required": [
    "schema",
    "schema_version",
    "generated_at",
    "before_sha",
    "after_sha",
    "min_run_count",
    "arms",
    "evidence_errors"
  ],
  "unevaluatedProperties": false,
  "properties": {
    "schema": {
      "const": "e2e_performance_benchmark_manifest_v1"
    },
    "schema_version": {
      "const": 1
    },
    "generated_at": {
      "type": "string",
      "minLength": 1,
      "format": "date-time"
    },
    "before_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "after_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "min_run_count": {
      "type": "integer",
      "minimum": 1
    },
    "arms": {
      "type": "object",
      "required": [
        "before",
        "after"
      ],
      "unevaluatedProperties": false,
      "properties": {
        "before": {
          "$ref": "#/$defs/Arm"
        },
        "after": {
          "$ref": "#/$defs/Arm"
        }
      }
    },
    "evidence_errors": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/EvidenceError"
      }
    }
  },
  "$defs": {
    "Arm": {
      "type": "object",
      "required": [
        "commit_sha",
        "jobs",
        "complete",
        "topology"
      ],
      "unevaluatedProperties": false,
      "properties": {
        "commit_sha": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$"
        },
        "jobs": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/JobCohort"
          }
        },
        "complete": {
          "type": "boolean",
          "description": "True only if every job's deduped workflow_run_id sample count is >= min_run_count."
        },
        "topology": {
          "type": "string",
          "enum": [
            "pre_split",
            "post_split"
          ],
          "description": "#2159 P0-4: pre_split (before, monolithic e2e job) vs post_split (after, e2e-core/e2e-responsive-matrix)."
        }
      }
    },
    "JobCohort": {
      "type": "object",
      "required": [
        "job",
        "run_count",
        "sample_workflow_run_ids",
        "runs"
      ],
      "unevaluatedProperties": false,
      "properties": {
        "job": {
          "type": "string",
          "minLength": 1
        },
        "run_count": {
          "type": "integer",
          "minimum": 0,
          "description": "Deduped (by workflow_run_id) sample count -- rerun attempts of the same workflow_run_id never add an independent sample (Issue #2159 AC2/P1-1)."
        },
        "sample_workflow_run_ids": {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 1
          },
          "uniqueItems": true
        },
        "runs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RunRecord"
          }
        }
      }
    },
    "RunRecord": {
      "type": "object",
      "required": [
        "workflow_run_id",
        "job",
        "head_sha",
        "artifact_id",
        "artifact_digest",
        "conclusion",
        "workflow_digest",
        "workflow_sha"
      ],
      "unevaluatedProperties": false,
      "properties": {
        "workflow_run_id": {
          "type": "integer",
          "minimum": 1
        },
        "job": {
          "type": "string",
          "minLength": 1
        },
        "head_sha": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$"
        },
        "artifact_id": {
          "type": "integer",
          "minimum": 1
        },
        "artifact_digest": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$"
        },
        "conclusion": {
          "type": "string",
          "enum": [
            "success",
            "failure",
            "cancelled",
            "skipped",
            "timed_out"
          ]
        },
        "workflow_digest": {
          "type": "string",
          "minLength": 1,
          "description": "#2159 P0-9/AC1: content identity of the workflow definition that produced this run (distinct from the measured-code head_sha)."
        },
        "workflow_sha": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$",
          "description": "#2159 OWNER scope-authority ruling (issuecomment-5299412215, item 1/P0-2): the commit SHA of the WORKFLOW DEFINITION that produced this run (`GITHUB_WORKFLOW_SHA`, i.e. the ref the run was dispatched against), distinct from both `head_sha` (the measured application code's commit, checked out via an explicit `actions/checkout` `ref:` override) and `workflow_digest` (a content hash of the workflow file). For a fixed-SHA benchmark dispatch these two SHAs are EXPECTED to differ: `workflow_sha` stays on the current/default branch tip so the workflow's OWN instrumentation logic is current, while `head_sha` pins the OLD/NEW application code being measured."
        },
        "run_attempt": {
          "type": "integer",
          "minimum": 1,
          "description": "#2179 AC4: the GitHub Actions rerun-attempt number (`GITHUB_RUN_ATTEMPT`) SELECTED for this sample under `rerun_attempt_selection_policy` -- always 1 under the current `initial_attempt_only_v1` policy (a `workflow_run_id` whose attempt 1 failed/is missing is excluded from the cohort entirely, never substituted with a later attempt). Backward-compatible OPTIONAL field: manifests produced before #2179 omit it."
        },
        "rerun_attempt_selection_policy": {
          "enum": ["initial_attempt_only_v1"],
          "description": "#2179 AC3/AC4 (narrowed by #2182 fix_delta hygiene item, OWNER adversarial review issuecomment-5302446086): identifies the deterministic, order-independent policy used to select `run_attempt` for this sample. Restricted to the single currently-recognized policy name -- an unrecognized policy string is never silently accepted as if it were a known policy. Backward-compatible OPTIONAL field: manifests produced before #2179 omit it."
        },
        "measured_head_sha": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$",
          "description": "#2184 AC1/AC3: the commit ACTUALLY checked out and measured, observed independently by the collecting step itself via `git rev-parse HEAD` -- never a direct copy of the `workflow_dispatch` `target_sha` input. DISTINCT from `head_sha` above, which resolves to `github.sha` (the workflow_dispatch dispatch ref's tip, NOT `target_sha`) on a fixed-SHA benchmark dispatch; the two fields are never asserted equal. Backward-compatible OPTIONAL field: a record produced before #2184 omits it, and this field is never inferred/synthesized for such a legacy record."
        },
        "workflow_run_head_sha": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$",
          "description": "#2184 AC2/AC3 (narrowed by fix_delta after OWNER adversarial review of PR #2493, issuecomment-5540651128): the head SHA of the GitHub Actions workflow run itself, derived SOLELY from the raw `ci_runtime_baseline_v1` artifact's existing `merge_sha` field (`GH_SHA: ${{ github.sha }}`, unchanged by this Issue -- no new producer field is introduced for this value); a caller-supplied `workflow_run_head_sha` field on a raw input record is never consulted and can never override this derivation. Live-API verification (`verify_run_record_against_live_api`) checks this field for SELF-CONSISTENCY against `api_workflow_run.head_sha` / `api_job.head_sha`, never against `measured_head_sha`/target_sha. Backward-compatible OPTIONAL field, present in the output `RunRecord` whenever `merge_sha` is derivable: a #2159-era pre-#2184 record that already carried `merge_sha` (recorded unconditionally since #2159, before #2184's `measured_head_sha` field existed) still yields a derived `workflow_run_head_sha` here; only a genuinely legacy record lacking `merge_sha` entirely omits this field (never inferred/synthesized in that case). For `e2e-core`/`e2e-responsive-matrix` records specifically, a record lacking BOTH `measured_head_sha` and a `merge_sha`-derivable `workflow_run_head_sha` is excluded from the trusted cohort with the `legacy_ambiguous_head_sha` evidence_errors reason (see `LEGACY_AMBIGUOUS_HEAD_SHA_JOBS`)."
        }
      }
    },
    "EvidenceError": {
      "type": "object",
      "required": [
        "arm",
        "reason",
        "detail"
      ],
      "unevaluatedProperties": false,
      "properties": {
        "arm": {
          "type": "string",
          "enum": [
            "before",
            "after"
          ]
        },
        "reason": {
          "type": "string",
          "minLength": 1
        },
        "detail": {
          "type": "string",
          "minLength": 1
        }
      }
    }
  }
}
