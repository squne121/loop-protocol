[project]
name = "loop-protocol-tools"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8.0",
  "pyyaml>=6.0",
  "jsonschema>=4.0",
  "ruff>=0.5",
]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"

[tool.ruff]
line-length = 120
