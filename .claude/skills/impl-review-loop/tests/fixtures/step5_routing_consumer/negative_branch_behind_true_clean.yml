description: branch_behind_main=true but merge_state_status=CLEAN → AC6 invariant
  violation (fail-closed)
loop_verdict:
  verdict: APPROVE
  merge_ready: false
  reviewed_head_sha: abc123def456
  mergeability:
    mergeable: MERGEABLE
    merge_state_status: CLEAN
  required_auto_actions:
  - kind: update_branch
    executor: implementation-worker
    skill: implement-issue.update_branch
    blocking_merge_ready: true
    mechanical: true
    expected_head_sha: abc123def456
test_verdict:
  branch_behind_main: true
expected:
  route: fail_closed
  fail_closed: true
  reason_code_prefix: branch_behind_invariant_violation
