# textlint ignore patterns
# .claude/ contains agent/skill definitions and fixture files that use
# controlled vocabulary (AC, VC, etc.) in structured formats — excluded
# from prose lint to avoid false positives on fixture/template content.
.claude/**
# docs/ and .github/ contain existing content with prh vocabulary that
# predates the textlint introduction — excluded to avoid false positives
# on existing terminology. New docs should comply with textlint rules.
docs/**
.github/**
# Exception: docs/dev/glossary.md is a new file that complies with textlint rules
# and should be validated. Negation pattern to re-include from the docs/** exclusion above.
!docs/dev/glossary.md
