# textlint ignore patterns
# .claude/ contains agent/skill definitions and fixture files that use
# controlled vocabulary (AC, VC, etc.) in structured formats — excluded
# from prose lint to avoid false positives on fixture/template content.
.claude/**
